Run bash deploy-all.sh us-central1
🚀 Deploying API Service (stockrecs)...
── Rebuilding frontend (esbuild) ──
  app.compiled.js      256.2kb
  app.compiled.js.map  586.9kb

⚡ Done in 35ms
[build] wrote app.compiled.js (256.2 KB) in 43 ms
✅ frontend rebuilt
── Running pre-deploy ruff lint ──
217 |         "source": "bull_thesis",
    |

services/correlation.py:154:58: F821 Undefined name `Any`
    |
154 | def markowitz_covariance_multiplier(new_ticker: str, db: Any) -> float:
    |                                                          ^^^ F821
155 |     """
156 |     Calculates the Marginal Contribution to Risk (MCTR) of adding the new ticker
    |

services/live_quotes.py:123:72: F821 Undefined name `pd`
    |
123 | def get_recent_trades_df(ticker: str, max_age_sec: float) -> Optional["pd.DataFrame"]:
    |                                                                        ^^ F821
124 |     """Zero-latency read of the live in-memory tape for ML microstructure extraction."""
125 |     import pandas as pd
    |

Found 11 errors.
❌ ruff found issues — aborting. Set SKIP_LINT=1 to override.
   To run locally: cd backend && ruff check .
Error: Process completed with exit code 1.