import pandas as pd
from typing import List, Dict


def detect_patterns(df: pd.DataFrame) -> List[Dict]:
    return []

def _double_top_bottom(df: pd.DataFrame, window: int = 20, tol: float = 0.02) -> List[Dict]: return []
def _head_and_shoulders(df: pd.DataFrame) -> List[Dict]: return []
def _triangle(df: pd.DataFrame) -> List[Dict]: return []
def _flag(df: pd.DataFrame) -> List[Dict]: return []
def _golden_death_cross(df: pd.DataFrame) -> List[Dict]: return []
